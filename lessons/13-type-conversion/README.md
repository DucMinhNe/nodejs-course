# Lesson 13: Type conversion

Explicit conversion vs implicit coercion.

## What you'll learn

- explicit: `String(x)`, `Number(x)`, `Boolean(x)`
- implicit: `'5' * 2 === 10`, `1 + '2' === '12'`

## Example

See `examples/`.

## Exercise

Predict the output of 6 mixed coercion expressions.

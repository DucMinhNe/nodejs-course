# Lesson 15: Comparison operators

==, ===, !=, !==, <, >, <=, >=.

## What you'll learn

- `===` strict equality (no coercion) — prefer this
- `==` loose equality (coerces types) — avoid

## Example

See `examples/`.

## Exercise

Explain why '10' < '9' is true.

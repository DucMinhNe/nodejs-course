# Lesson 17: if / else if / else

Branching based on conditions.

## What you'll learn

- condition is coerced to boolean
- chain with `else if`
- prefer early returns (guard clauses) to deep nesting
- use `===` in conditions

## Example

See `examples/`.

## Exercise

Write a function that classifies a number as negative/zero/positive.

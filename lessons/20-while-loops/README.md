# Lesson 20: while & do-while

Loop while a condition holds.

## What you'll learn

- `while (cond) { ... }` checks before each iteration
- `do { ... } while (cond)` runs at least once

## Example

See `examples/`.

## Exercise

Use a while loop to sum digits of a number.

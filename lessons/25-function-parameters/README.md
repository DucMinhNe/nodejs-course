# Lesson 25: Function parameters

Default values, rest params, and destructured params.

## What you'll learn

- default: `function f(x = 10) {}`
- rest: `function f(...args) {}` collects extras into an array

## Example

See `examples/`.

## Exercise

Write a `range(start, end, step = 1)` with a default step.

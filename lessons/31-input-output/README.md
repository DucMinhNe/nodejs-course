# Lesson 31: Input & output

Reading from stdin, writing to stdout/stderr.

## What you'll learn

- `console.log` → stdout; `console.error` → stderr
- `process.stdout.write` for no trailing newline
- `readline` for interactive line-by-line input
- pipe data through `process.stdin`

## Example

See `examples/`.

## Exercise

Build a tiny CLI that asks two numbers and prints the sum.

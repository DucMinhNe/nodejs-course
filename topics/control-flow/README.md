# Control Flow

if/else, ternary, switch.

## Key points

- ternary is fine for one-liners — avoid nesting
- `switch` matches with `===`, falls through without `break`

## Examples

See `examples/` for runnable demos.

## Exercise

Replace a long `switch` with an object lookup.

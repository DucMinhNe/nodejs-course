# Currying & HOFs

Currying transforms a multi-arg function into a chain of single-arg ones.

## Key points

- partial application via .bind
- generic curry helpers

## Examples

See `examples/` for runnable demos.

## Exercise

Write `pipe(...fns)` that composes left-to-right.

See `exercise.js` for the starter.

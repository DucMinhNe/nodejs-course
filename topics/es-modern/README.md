# Modern JS features

Recent ECMAScript additions worth knowing.

## Key points

- optional chaining `?.` and nullish coalescing `??`
- logical assignment `||=`, `??=`, `&&=`

## Examples

See `examples/` for runnable demos.

## Exercise

Replace `Object.prototype.hasOwnProperty.call(o, 'x')` with `Object.hasOwn`.

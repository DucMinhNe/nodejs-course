# Express Basics

Routing, parameters, query strings, request body.

## Key points

- `express.json()` middleware to parse JSON bodies
- route params: `/users/:id`

## Examples

See `examples/` for runnable demos.

## Exercise

Build a basic CRUD for an in-memory todo list.

See `exercise.js` for the starter.

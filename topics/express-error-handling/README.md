# Express Error Handling

Centralised error middleware + async-safe handlers.

## Key points

- async errors must be passed to `next(err)`
- use an `asyncHandler` wrapper or `express-async-errors`

## Examples

See `examples/` for runnable demos.

## Exercise

Build an `asyncHandler` helper and use it on three routes.

See `exercise.js` for the starter.

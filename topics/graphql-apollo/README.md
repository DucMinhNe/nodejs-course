# GraphQL with Apollo

Schema-first APIs with typed resolvers.

## Key points

- one endpoint, client-driven queries
- resolvers map (parent, args, context) → value

## Examples

See `examples/` for runnable demos.

## Exercise

Build a tiny User/Post schema.

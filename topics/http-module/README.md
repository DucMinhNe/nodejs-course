# Built-in HTTP

Node ships with a low-level `http` module. Most apps use a framework on top.

## Key points

- server: `http.createServer((req, res) => ...)`
- client: `http.request` / `https.get`

## Examples

See `examples/` for runnable demos.

## Exercise

Build a tiny static file server using only the built-in `http` and `fs` modules.

See `exercise.js` for the starter.

# MongoDB / Mongoose

Mongoose models a schema layer on top of MongoDB.

## Key points

- define a `Schema`, register a `Model`
- queries return Mongoose Documents (not plain objects) by default

## Examples

See `examples/` for runnable demos.

## Exercise

Build a User model with email index and a `findByEmail` static.

See `exercise.js` for the starter.

# Spread & Rest

`...` does spread (expand) or rest (collect), depending on context.

## Key points

- spread in calls: `fn(...args)`
- spread in literals: `[...a, ...b]`, `{ ...obj1, ...obj2 }`

## Examples

See `examples/` for runnable demos.

## Exercise

Implement a clone helper using spread (shallow only).

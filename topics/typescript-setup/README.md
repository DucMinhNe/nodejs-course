# TypeScript with Node

Set up TypeScript for a Node project.

## Key points

- `tsc --init` → tsconfig.json
- `ts-node` for dev (no build step) — or `tsx` for faster

## Examples

See `examples/` for runnable demos.

## Exercise

Configure a Node ESM-style TS project.

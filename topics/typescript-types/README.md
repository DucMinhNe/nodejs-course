# TypeScript types deep

Beyond basic types: generics, conditional types, utility types.

## Key points

- utility types: Partial, Required, Pick, Omit, Record, ReturnType
- conditional types: `T extends U ? X : Y`

## Examples

See `examples/` for runnable demos.

## Exercise

Write a type-safe `pick` helper.
